<Window x:Class="ChatClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatClient"
        mc:Ignorable="d"
        Title="Chat" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Список пользователей -->
        <GroupBox Header="Users" Grid.Column="0" Margin="6">
            <ListBox ItemsSource="{Binding Users}" />
        </GroupBox>

        <!-- Сообщения и ввод -->
        <Grid Grid.Column="1" Margin="6">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0">
                <ItemsControl ItemsSource="{Binding Messages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="4">
                                <TextBlock FontWeight="Bold" Text="{Binding Username}"/>
                                <TextBlock Text="{Binding Text}" TextWrapping="Wrap"/>
                                <TextBlock FontSize="10" Text="{Binding Time}" HorizontalAlignment="Right"/>
                                <Separator/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,0">
                <TextBox Text="{Binding MessageText, UpdateSourceTrigger=PropertyChanged}" Width="600" Height="30"/>
                <Button Content="Send" Command="{Binding SendCommand}" Width="80" Margin="8,0,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
